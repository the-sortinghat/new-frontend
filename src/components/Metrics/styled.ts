import styled from "styled-components";

export const MetricsWrapper = styled.aside`
  width: 100%;
  height: 100vh;
  overflow-y: scroll;
  padding-left: 1rem;

  @media (max-width: 1000px) {
    margin-top: 2rem;
    padding: 0;
  }
`;

export const Title = styled.h2`
  margin-top: 0;
`;

export const MetricsType = styled.h4``;
